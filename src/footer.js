import React from 'react';

const Footer=(props) =>{
        return(
            <div >
                <span className="footer">Rs {props.total}</span>
            </div>
        )
}

export default Footer;